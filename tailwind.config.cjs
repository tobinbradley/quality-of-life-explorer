const config = {
  content: ["*.html", "./src/**/*.{html,js,svelte,ts}"],

  theme: {
    extend: {
      colors: {
        'highlight': "#be185d"
      }
    }
  },

  plugins: [],
};

module.exports = config;
