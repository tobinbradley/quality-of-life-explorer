const config = {
  content: ["*.html", "./src/**/*.{html,js,svelte,ts}"],

  theme: {
    extend: {
      colors: {
        'highlight': "#DB2777"
      }
    }
  },

  plugins: [],
};

module.exports = config;
